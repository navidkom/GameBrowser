package ir.artapps.gamebrowser.ui.social

import androidx.lifecycle.ViewModel
import ir.artapps.gamebrowser.repo.ChatRepository

class SocialViewModel(chatRepository: ChatRepository) : ViewModel(){

    val dummy = false
}